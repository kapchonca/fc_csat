[project]
name = "fc-csat"
version = "0.1.0"
description = "Synthetic dialogue generator."
requires-python = ">=3.12"
dependencies = ["tqdm>=4.66"]

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
